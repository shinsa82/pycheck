"""
Experiments on variable scope, especially scopes with which when eval() is 
called to generate functions.

normal_grobal.py - normal eval with implicit globals.
injected_global.py - eval() with custom globals and it'll updated later.
"""
